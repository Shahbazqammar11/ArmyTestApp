import { StyleSheet, Text, View } from "react-native";
import React from "react";

const db = () => {
  return (
    <View>
      <Text>db</Text>
    </View>
  );
};

export default db;

const styles = StyleSheet.create({});
